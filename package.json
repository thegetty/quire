{
  "name": "root",
  "private": true,
  "version": "0.0.0",
  "description": "a multi-format book publishing framework",
  "author": "Getty Digital",
  "license": "MIT",
  "scripts": {
    "install:cli": "npm run build --prefix packages/cli && npm install -g packages/cli",
    "postinstall": "lerna bootstrap --hoist",
    "test": "jest",
    "test:cli": "npm test --prefix packages/cli",
    "uninstall:cli": "cd packages/cli && npm uninstall -g"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thegetty/quire.git"
  },
  "bugs": {
    "url": "https://github.com/thegetty/quire/issues"
  },
  "homepage": "https://quire.getty.edu/",
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {}
}
