# quire-cli
Command line interface for building and managing Quire projects

More info TK

## Installation
To have a globally available `quire` command, run `npm install -g` inside the project folder after cloning this repo. 
To create a symbolic link (useful if you are developing locally) run `npm link` in the project directory.
