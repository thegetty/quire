{% if forloop.first %}
  {% render "contrib-name", contributor: contributor %}
{%- elsif forloop.length === 2 %} and {% render "contrib-name-last", contributor: contributor%}
{%- endif -%}
{%- if forloop.length > 2 %}, et al{% endif -%}
