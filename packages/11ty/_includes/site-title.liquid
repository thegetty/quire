{% comment %}
Concatenates the site title and subtitle, using a colon, or if the title ends with a ! or ?, no colon is included.

See also page/title.liquid
{% endcomment %}

{% assign lastLetter = publication.title | split "" | last %}
{% unless "!?" | split "" contains lastLetter %}
  {% assign endPunctuation = ":" %}
{% endif %}

{{ publication.title }}{{endPunctuation}}{% if publication.subtitle %} {{ publication.subtitle }}{% endif %}{% if publication.readingLine %} ({{ publication.readingLine }}){% endif %}
