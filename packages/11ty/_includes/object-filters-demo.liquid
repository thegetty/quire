<style type="text/css" media="screen">
/* GLOBAL STYLES */
.object-filters {
  font-size: 14px;
}

.object-filter {
  font-size: 14px;
}

.object-filter label {
  font-family: Noto Sans,sans-serif;
  font-size: 14px;
  font-weight: 700;
  color: rgb(51, 51, 51);
}

.object-filter select,
.object-filter select:focus,
.object-filter select:focus-visible {
  border: none;
  border-bottom: 1px solid #d0dbde;
  color: rgb(51, 51, 51);
  background: transparent;
  min-width: 10em;
  font-family: Noto Sans,sans-serif;
  font-size: 14px;
}

.object-filters__button {
  border-radius: 2px;
  border: 1px solid rgb(51, 51, 51);
  background: #eef2f3;
  /* background: rgb(51, 51, 51); */
  color: rgb(51, 51, 51);
  /* color: rgb(255, 255, 255); */
  font-family: Noto Sans,sans-serif;
  font-size: 14px;
  height: 2em;
  padding: 0.1em 2em 0.15em;
  cursor: pointer;
}

.object-filters__button.object-filters__button--active {
  background: rgb(51, 51, 51);
  color: white;
}

/* IN-PAGE FILTERS */

/* min-width: 700px for horizontal */
.quire-page__object-filters {
  --max-height: 25vh;
  /* padding: 1em 0; */
  /* border-top: 1px solid rgb(51, 51, 51); */
  /* display: flex; */
  padding: 0 28.5px;
  background: #eef2f3;
  /* justify-content: center; */
  display: grid;
}

.quire-page__object-filters.sticky {
  position: sticky;
  z-index: 1;
  top: 50px;
}

.quire-page__object-filters .object-filters {
  padding-top: 1em;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(auto, 1fr));
  grid-row-gap: 0.8em;
  grid-column-gap: 3rem;
}

.quire-page__object-filters .object-filter {
  display: grid;
  grid-template-columns: auto;
  grid-gap: 0.5em;
  align-items: end;
  max-width: 30em;
  justify-self: center;
  width: 100%;
}

@media screen and (min-width: 500px) {
  .quire-page__object-filters .object-filter {
    max-width: unset;
    grid-template-columns: minmax(auto, 1fr) minmax(auto, 2fr);
  }

  .quire-page__object-filters .object-filter label {
    text-align: right;
  }
}

@media screen and (min-width: 700px) {
  .quire-page__object-filters {
    margin-bottom: 3rem;
  }

  .quire-page__object-filters .object-filters {
    grid-template-columns: repeat(auto-fit, minmax(30em, 1fr));
  }

  .quire-page__object-filters .object-filter {
    grid-template-columns: minmax(auto, 1fr) minmax(auto, 2fr) minmax(auto, 1fr);
  }
}

.quire-page__object-filters .object-filters__button {
  margin-top: 2em;
  align-self: center;
}

/* COLLAPSIBLE */

.collapsible {
  position: relative;
}

.collapsible-checkbox {
  display: none;
}

.collapsible .collapsible-toggle {
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
  background: linear-gradient(0deg, rgba(238,242,243,1) 0%, rgba(238,242,243,1) 33%, rgba(238,242,243,0) 100%);
  border-bottom: 2px solid #d0dbde;
  height: 1em;
  width: calc(100% - (2 * 27px));
  bottom: 0;
}

.collapsible .collapsible-toggle::before {
  content: 'Show All Filters';
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translate(-50%, 100%);
  display: inline-block;
  background: #d0dbde;
  padding: 0.2em 0.5em;
  border-bottom-left-radius: 2px;
  border-bottom-right-radius: 2px;
  color: white;
  font-size: 75%;
}

.collapsible .collapsible-checkbox:checked ~ .collapsible-toggle::before {
  content: 'Collapse Filters';
  transform: translate(-50%, 0);
  border-top-left-radius: 2px;
  border-top-right-radius: 2px;
  border-bottom-left-radius: 0;
  border-bottom-right-radius: 0;
}

.collapsible-panel {
  display: flex;
  flex-direction: column;
  padding-bottom: 1em;
}

.collapsible .collapsible-checkbox:not(:checked) ~ .collapsible-panel {
  /* height: calc(var(--max-height) / 2); */
  height: 10em;
  overflow: hidden;
}

.collapsible .collapsible-checkbox:checked ~ .collapsible-panel {
  height: unset;
  padding-bottom: 3em;
}

.collapsible.sticky .object-filters{
  max-height: var(--max-height);
  overflow-y: scroll;
}

@media screen and (min-width: 500px) {
  .collapsible .collapsible-checkbox:not(:checked) ~ .collapsible-panel {
    height: 6.5em;
  }
}
</style>

<!--
     Class API
     .collapsible - sets  a `max-height`
     .sticky - sets `position: sticky` with an offset for primary nav
     .stuck - styles applied when sticky panel has 'stuck' to the top of the screen
-->
<section class="quire-page__object-filters">
  <input class="collapsible-checkbox" type="checkbox" id="collapsible-checkbox" />
  <label for="collapsible-checkbox" class="collapsible-toggle"></label>
  <div class="collapsible-panel">
    <div class="object-filters">
      <div class="object-filter">
        <label for="object-type">Type:</label>
        <select name="Type" id="object-type">
          <option value="">All</option>
        </select>
      </div>
      <div class="object-filter">
        <label for="object-theme">Theme:</label>
        <select name="Theme" id="object-theme">
          <option value="">All</option>
        </select>
      </div>
      <div class="object-filter">
        <label for="object-material">Material:</label>
        <select name="Material" id="object-material">
          <option value="">All</option>
        </select>
      </div>
      <div class="object-filter">
        <label for="object-place">Place:</label>
        <select name="Place" id="object-place">
          <option value="">All</option>
        </select>
      </div>
      <div class="object-filter">
        <label for="object-period">Time Period:</label>
        <select name="Time Period" id="object-period">
          <option value="">All</option>
        </select>
      </div>
      <div class="object-filter">
        <label for="object-period">Ut placerat orci nulla pellentesque dignissim enim, sit amet venenatis urna cursus eget nunc</label>
        <select name="Time Period" id="object-period">
          <option value="">Sed id semper risus in hendrerit gravida rutrum quisque</option>
        </select>
      </div>
      <div class="object-filter">
        <label for="object-type">Type:</label>
        <select name="Type" id="object-type">
          <option value="">All</option>
        </select>
      </div>
      <div class="object-filter">
        <label for="object-theme">Theme:</label>
        <select name="Theme" id="object-theme">
          <option value="">All</option>
        </select>
      </div>
      <div class="object-filter">
        <label for="object-material">Material:</label>
        <select name="Material" id="object-material">
          <option value="">All</option>
        </select>
      </div>
      <div class="object-filter">
        <label for="object-place">Place:</label>
        <select name="Place" id="object-place">
          <option value="">All</option>
        </select>
      </div>
      <div class="object-filter">
        <label for="object-period">Time Period:</label>
        <select name="Time Period" id="object-period">
          <option value="">All</option>
        </select>
      </div>
    </div>
    <button class="object-filters__button">Reset Filters</button>
  </div>
</section>
<script type="text/javascript">
const classes = [
  'collapsible',
  // 'sticky',
  // 'stuck'
];
window.addEventListener('load', () => {
  const element = document.querySelector('.quire-page__object-filters');
  element.classList.add(...classes);
});
</script>
