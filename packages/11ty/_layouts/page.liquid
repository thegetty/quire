---
classes:
  - quire-page
layout: base.11ty.js
description: Single page default template
---

{% pageHeader
  byline_format=byline_format,
  image=image,
  label=label,
  pageContributors=pageContributors,
  subtitle=subtitle,
  title=title,
  page_pdf_output=page_pdf_output,
  key=key,
  outputs=outputs
%}
{% if content %}
<section class="section quire-page__content">
  <div class="container">
    <div class="content">
      {{ content }}
      {% bibliography citations %}

      {% comment %}
        NB: Reivisit this in 11ty 3.0.
        There's a lot of logic coming together in this component -- page data and the project pdf config, plus user-generated content in the key. Control looks inverted.
      {% endcomment %}
      {% footerDownloadLink 
         key=key
         outputs=outputs 
         page_pdf_output=page_pdf_output
      %}
    </div>
  </div>
</section>
{% endif %}
<section class="section quire-page__content">
  <div class="container">
    {% pageButtons pagination=pagination %}
  </div>
</section>
