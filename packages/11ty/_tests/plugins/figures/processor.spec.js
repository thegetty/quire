import test from 'ava'

// TODO: Create some test fixture images at various sizes (100x100, 200x100, and so on)

test('Processor should correctly handle composite overlays', async (t) => {
  // TODO: Test that the canvas is always at the size of the base image
  // TODO: Test that 3 same-sized images get overlaid without error

  t.fail()
})

test('Processor should correctly handle composite grids', async (t) => {
  // TODO: Test that the canvas matches expected dimensions
  // TODO: Test that a grid is successfully created no matter the order of the images

  t.fail()
})
